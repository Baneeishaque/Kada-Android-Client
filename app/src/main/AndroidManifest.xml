<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ndk.kada">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kada.NoActionBar"
        tools:replace="android:allowBackup">
        <activity
            android:name=".activities.ShopItineraryActivity"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".activities.ShopDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.OtpActivity"
            android:exported="true" />
        <activity
            android:name=".activities.StoreStockActivity"
            android:exported="true" />
        <activity
            android:name=".activities.StoreDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".activities.StorePortalHomeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LocationDecidedActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LocationRequestActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SurroundingsPlottedActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library android:name="org.apache.http.legacy" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>