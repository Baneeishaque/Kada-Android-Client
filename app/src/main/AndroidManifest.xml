<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ndk.kada">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kada.NoActionBar"
        tools:replace="android:allowBackup">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />

        <activity
            android:name=".ShopItemAdditionActivity"
            android:exported="true" />

        <activity
            android:name=".ShopDetailsActivity"
            android:exported="true" />

        <activity
            android:name=".OtpActivity"
            android:exported="true" />
        <activity
            android:name=".StoreStockActivity"
            android:exported="true" />
        <activity
            android:name=".AddStoreProductActivity"
            android:exported="true" />
        <activity
            android:name=".StoreDashboardActivity"
            android:exported="true" />
        <activity
            android:name=".StorePortalHomeActivity"
            android:exported="true" />
        <activity
            android:name=".LocationDecidedActivity"
            android:exported="true" />
        <activity
            android:name=".LocationRequestActivity"
            android:exported="true" />
        <activity
            android:name=".SurroundingsPlottedActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library android:name="org.apache.http.legacy" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>